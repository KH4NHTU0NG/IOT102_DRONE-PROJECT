# Mosquitto MQTT Broker — Windows config
# Kiểm chứng: Windows 10/11 + Docker Desktop (WSL2 backend)

listener 1883
allow_anonymous true

# Logging
log_type all
log_dest stdout

# Persistence (disabled để tránh conflict container restart)
persistence false
