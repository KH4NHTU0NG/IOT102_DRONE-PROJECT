# Mosquitto MQTT Broker — macOS Apple Silicon config
# Kiểm chứng: macOS M-series + Docker Desktop ARM

listener 1883
allow_anonymous true

# Logging
log_type all
log_dest stdout

# Persistence (disabled để tránh conflict container restart)
persistence false
